// Stub for environments outside Next.js (unit tests import src/lib/validation
// which transitively imports src/lib/api → src/lib/auth → "server-only").
// The real package simply throws in client bundles; here it is a no-op.
export {};
